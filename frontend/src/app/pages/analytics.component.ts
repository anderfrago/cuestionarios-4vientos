import { Component, OnInit, inject, signal } from '@angular/core';
import { DatePipe } from '@angular/common';
import { ActivatedRoute } from '@angular/router';
import { ApiService } from '../core/api.service';
import { Attempt, Course } from '../core/models';
@Component({standalone:true,imports:[DatePipe],template:`<div class="container py-5">@if(course()){<div class="d-flex flex-wrap justify-content-between align-items-end mb-4"><div><p class="text-primary fw-semibold mb-1">Tutoría · {{course()?.academic_year}}</p><h1 class="h2 mb-0">{{course()?.name}}</h1></div><a class="btn btn-outline-primary" [href]="'/api/courses/'+course()?.id+'/export.csv'">Exportar CSV</a></div><div class="row g-4 mb-5">@for(s of summary();track s.aspect){<div class="col-md-4"><div class="card shadow-sm p-4"><div class="display-score">{{s.average}}</div><strong>{{s.aspect}}</strong><span class="small text-secondary">{{s.count}} mediciones</span></div></div>}</div><h2 class="h4">Resultados individuales y evolución</h2><div class="table-responsive card shadow-sm"><table class="table align-middle mb-0"><thead><tr><th>Alumno/a</th><th>Fecha</th><th>Resultados</th></tr></thead><tbody>@for(a of attempts();track a.id){<tr><td><strong>{{a.student.name}}</strong><div class="small text-secondary">{{a.student.email}}</div></td><td>{{a.created_at|date:'dd/MM/yyyy HH:mm'}}</td><td>@for(r of a.results;track r.aspect_id){<span class="badge rounded-pill text-bg-light me-1">{{r.aspect}} {{r.average}} · {{r.level}}</span>}</td></tr>}@empty{<tr><td colspan="3" class="text-center py-5">Todavía no hay respuestas.</td></tr>}</tbody></table></div>}</div>`})
export class AnalyticsComponent implements OnInit {private api=inject(ApiService);private route=inject(ActivatedRoute);course=signal<Course|null>(null);summary=signal<{aspect:string;average:number;count:number}[]>([]);attempts=signal<Attempt[]>([]);ngOnInit(){this.api.analytics(Number(this.route.snapshot.paramMap.get('id'))).subscribe(r=>{this.course.set(r.course);this.summary.set(r.summary);this.attempts.set(r.attempts)})}}

